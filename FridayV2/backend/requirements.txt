# Core
anthropic>=0.40.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Supabase — pinned to 2.9.1 to avoid pyiceberg C-extension build failure
supabase==2.9.1

# Telegram
python-telegram-bot[job-queue]>=21.0

# Google APIs
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0
google-cloud-texttospeech>=2.0.0

# LiveKit — pinned to prevent pip crawling hundreds of versions (0.12.0 → 1.5.x)
livekit-agents==1.5.17
livekit-plugins-deepgram==1.5.17
livekit-plugins-anthropic==1.5.17
livekit-plugins-google==1.5.17

# HTTP
requests>=2.31.0
tavily-python>=0.3.0
garminconnect>=0.3.0

# Dashboard API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Timezone data
tzdata>=2024.1
